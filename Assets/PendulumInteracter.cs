using UnityEngine;
// لم نعد نحتاج نظام الإدخال الجديد هنا
// using UnityEngine.InputSystem; 

public class PendulumInteracter : MonoBehaviour
{
    // ربط مباشر مع سكربت الحبل (لم نعد نحتاجه للدفع اليدوي)
    // public CustomVerletPendulum pendulum;

    void Update()
    {
        // تم حذف منطق رصد زر Space، السكربت الآن لا يفعل شيئاً.
    }
}